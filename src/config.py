import keys
debug = False
idle = True

token = keys.token
mail = keys.mail
password = keys.password
host = 'imap.gmail.com'

if debug :
    chat_id = keys.debug_chat_id
else :
    chat_id = keys.chat_id
